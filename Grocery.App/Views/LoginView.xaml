<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Grocery.App.ViewModels"
             x:Class="Grocery.App.Views.LoginView"
             x:DataType="vm:LoginViewModel"
             Title="LoginView">
    <StackLayout Padding="20" VerticalOptions="Center">
        <Label Text="Login" FontSize="24" HorizontalOptions="Center" Margin="0,0,0,50" />
        <Entry Placeholder="Email" Text="{Binding Email}" Keyboard="Email" WidthRequest="400" />
        <Entry Placeholder="Password" Text="{Binding Password}" WidthRequest="400"  IsPassword="True" />
		<Entry Placeholder="Naam (optioneel)" Text="{Binding Name}" WidthRequest ="400"/>
		<StackLayout Orientation="Horizontal"
                 Spacing="20"
                 HorizontalOptions="Center"
                 Margin="0,30,0,0">
            <Button Text="Login"
                Command="{Binding LoginCommand}"
                WidthRequest="200"
                HeightRequest="55"
                CornerRadius="10"/>
            <Button Text="Registratie"
				Command="{Binding RegisterCommand}"
                WidthRequest="200"
                HeightRequest="55"
                CornerRadius="10"/>
        </StackLayout>
        <Label Text="{Binding LoginMessage}" TextColor="Red" />
    </StackLayout>
</ContentPage>